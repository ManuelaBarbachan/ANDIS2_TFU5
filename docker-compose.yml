version: '3.8'

services:
  # ========== MONOLITO==========
  monolito-api:
    build: ./monolito
    container_name: monolito-api
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/monolito_db
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - monolito-network
    restart: unless-stopped

  # ========== BD ==========
  postgres:
    image: postgres:16-alpine
    container_name: monolito-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: monolito_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - monolito-network
    restart: unless-stopped

networks:
  monolito-network:
    driver: bridge

volumes:
  postgres_data: